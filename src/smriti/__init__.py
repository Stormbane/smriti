"""smriti — a memory system for AI entities, grounded in the antahkarana model of mind.

See README.md and docs/ARCHITECTURE.md for the design.

v0.1.0-dev: read pipeline. Index the narada tree and search it with hybrid
vector + keyword retrieval. Install with ``pip install -e ".[read]"`` for
the full pipeline (sqlite-vec + onnxruntime).
"""

__version__ = "0.1.0-dev"
